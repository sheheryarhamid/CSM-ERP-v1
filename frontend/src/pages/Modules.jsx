import React from 'react'

export default function Modules(){
  return (
    <div>
      <h2>Modules</h2>
      <div className="card">Install/uninstall modules, view manifest details, and check compatibility. Module artifacts are validated server-side.</div>
    </div>
  )
}
