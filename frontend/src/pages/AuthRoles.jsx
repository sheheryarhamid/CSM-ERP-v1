import React from 'react'

export default function AuthRoles(){
  return (
    <div>
      <h2>Auth & Roles</h2>
      <div className="card">Manage users, roles, and session tokens. This UI will operate through the Hub's secure APIs only.</div>
    </div>
  )
}
