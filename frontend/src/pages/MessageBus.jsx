import React from 'react'

export default function MessageBus(){
  return (
    <div>
      <h2>Message Bus</h2>
      <div className="card">Publish messages to modules, view recent traffic, and configure delivery guarantees. This UI calls secure endpoints that gate module identity and scope.</div>
    </div>
  )
}
