import React from 'react'

export default function EnvironmentManager(){
  return (
    <div>
      <h2>Environment Manager</h2>
      <div className="card">Switch between SQLite, MySQL, PostgreSQL and test connections here (UI flows only).</div>
    </div>
  )
}
