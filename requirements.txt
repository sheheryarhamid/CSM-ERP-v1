# Backend Python dependencies (recommended latest stable versions)
fastapi
uvicorn[standard]
sqlalchemy
alembic
pydantic
sqlmodel
passlib[bcrypt]
PyJWT
cryptography
redis
aioredis
celery
httpx
pytest
prometheus-client
structlog
python-dotenv
# Manifest validator
jsonschema
# Optional helpers
# Explicitly pin brotli to avoid known decompression vulnerabilities (see reports/pip_audit.json)
brotli>=1.2.0
# (removed invalid package `alembic_autogenerate`) 

# Note: pin versions in production (use a constraints file or pyproject.toml)
